# Pre-release review — tosijs-ui 1.14.0 (base `v1.13.0`, 35 commits, depth=full)

**STATUS: CLEARED** (2026-09-08) — every finding below is dispositioned and this file is a
RECORD, not a gate. The blocker (`release-check` exit 1) and all five majors were fixed before
v1.14.0 shipped; F6–F15 landed across 1.14.0 and 1.14.1; U1 was filed upstream as
[tjs-lang#55](https://github.com/tonioloewald/tjs-lang/issues/55) and mirrored in
`UPSTREAM.md`; P1/P2 were written back to `tosijs-coding-practices` (commit `d8ae9d3`, naming
the range it covers). Anything still open lives in `TODO.md`, which is the only place open work
belongs. Left unedited below so the original assessment stays readable.

**ORIGINAL VERDICT: BLOCK** — one confirmed blocker (`bun run release-check` exits 1; reproduced), plus five verified majors, two of which sit on the #128 security hardening this release ships.

---

## Lane status (re-run at HEAD by this triage)

| Lane | Result |
| --- | --- |
| `bun test` (unit) | **1329 pass / 0 fail**, 86 files, 10.1s — green |
| `bunx tsc --noEmit` | exit 0 |
| `bunx tsc -p tsconfig.tests.json --noEmit` | exit 0 (**but 0 files under `tests/` — see F6**) |
| `bun run release-check` | **exit 1** — 17 unwritten annotations (**BLOCKER**) |
| `bun playwright test` / `test-browser` / `test-consumer` | **not re-run at HEAD** — see Completeness gaps |

One non-fatal console error is printed during `bun test`: `error: ENOENT reading "https://esm.sh/typescript@5"`. It does not fail a test (verified across the full run) but it is real and newly surfaced by this diff — see F10.

## Per-lens summary

| Lens | Outcome |
| --- | --- |
| **correctness** | 1 verified blocker (release gate red), 2 verified majors (dead `audit.blockOn`; stale audit-gate docs), 1 verified major with severity flagged uncertain (#93 preflight sees orphans but cannot flag them), 4 unverified minors/nits — 3 of which I confirmed at source during triage |
| **security** | 2 verified majors, both on the #128 remediation: the corpus-derived write allow-list admits executable build scripts this repo's own build spawns; the READ half of the same endpoint was never narrowed while the new docstring claims it was. Plus 2 unverified minors/nits |
| **blast-radius** | Same two majors found independently (deduped below); 1 verified major on the published audit-gate doc (deduped); 3 findings downgraded to minor on verification (unpinned esm.sh TypeScript, `dist/` cleaning docs, the breaking barrel removal's silent README) |
| **efficiency** | No blockers. 1 minor (the 1.35 MB barrel win is guarded only by a literal-string check; no lane weighs `dist/index.js`), 1 nit (the headline measurement is published as three different numbers) |
| **ecosystem** | **did not run** — and this release makes `@codemirror/state` + `@codemirror/view` part of the public API via `export *`. See Completeness gaps |
| **practices** | not run this pass |

---

## Blockers (must clear before `git tag`)

### B1 — `bun run release-check` exits 1: 17 commit annotations since v1.13.0 are unwritten
**`CHANGELOG.md`** · lens: correctness · **verified (reproduced during triage)**

Reproduced verbatim: `🛑 17 annotation(s) since v1.13.0 are not mentioned in CHANGELOG.md`, exit 1. The matcher is deliberately lenient (any surviving 3-word run, or any cited issue number, counts as covered), so these are genuine absences, not a strict-matcher artifact. Independent greps confirm `editableSourcePaths`, `mayEditSource`, `blockOn`, `runtimeReachable`, `classifyReach`, `getSelectText`, `WAIVER`, `firebase`, `#128`, `#139` have zero hits in the `## 1.14.0 (unreleased)` section, and every `tjs-lang` hit is in a pre-1.12 section.

The unwritten items are not cosmetic. Among them:
- `[fix]`/`[new]` **#128** — the `editableSources` write allow-list (a security narrowing an adopter's editor will feel as a new 403).
- `[fix]` **#135** — tjs-lang 0.13.4 was **npm-deprecated** and seven releases behind; `TJS_VERSION` is what the *published site* fetches from a CDN, so a reader's transpiler changed.
- `[new]` **#56** — `runtimeReachable`/`classifyReach`/`audit.blockOn` (see F3, which is the reason writing this up matters twice over).
- `[fix]` **#134** firebase.json/outputDir mismatch; `[fix]` **#139** executable-fence collision; `[fix]` `getSelectText` returned `''` on every call.

CLAUDE.md makes this gate's exit code the contract for publishing step 3.

**Fix:** write the 17 into the 1.14.0 section (or re-tag the genuinely internal ones `[note]`). Per CLAUDE.md's own loop-termination rule, the commit that does this generates its own annotations — land it with `[note]` bullets only, and re-run `release-check` as the **last** thing before `git tag`, since the range is exclusive of the since-commit and tagging turns the gate green without the bullet ever being written.

*(The security lens filed the missing #128 entry separately as a minor; it is the same defect and is folded in here.)*

---

## Majors — fix before the tag, or dispositioned explicitly (all verified)

### F1 — The #128 write allow-list still admits executable build scripts the build spawns every rebuild
**`src/doc-system/site/editable-sources.ts:25`** · lens: security · verified · severity flagged uncertain by the lens, kept at major

Against the **real shipped corpus** (not a synthetic one): `editableSourcePaths(demo/docs.json, repoRoot)` yields 66 paths, and `mayEditSource(<root>/bin/make-icon-data.js)` → **true**; `bin/release-notes.ts` → true. `bin/dev.ts:80` runs `await $\`bun ./bin/make-icon-data.js\`` in `prebuild` — every build and every watch rebuild. So a tunnel invitee holding a redeemed link session can POST `{file:'bin/make-icon-data.js', content:'<arbitrary JS>'}` to `/__docstore/source`, get 200, and have it execute with the developer's privileges at the next rebuild. That is verbatim the class `dev-server.ts:970` says the change closes (`bin/` is named there as one of the surfaces that "executes on the next ordinary command").

The unit test asserts `bin/dev.ts` is refused — but only because the 3-entry synthetic corpus omits `bin/` files. A corpus that documents its own tooling (which this project does, and recommends) puts them back. The hostile case — *a corpus entry that is itself an executed file* — is untested; the commit body's live verification used `.git/hooks/pre-commit`, which the corpus never contained.

Generalises to adopters: any project with `/*#` blocks in a build script hands write access to that script.

**Fix:** intersect the corpus-derived set with the doc source roots the endpoint needs (refuse anything outside `src/` + `docs/` + top-level markdown, or explicitly subtract `bin/` and anything the build spawns), and add a test whose corpus **contains** `bin/make-icon-data.js` and asserts refusal. If the residual is accepted instead, say so where the guarantee is stated — do not leave code claiming `bin/` is excluded.
*Re-review scope if fixed: correctness + security over the remediation diff only (the predicate and its tests).*

### F2 — Only the WRITE half of `/__docstore/source` was narrowed; the READ half serves any file under the repo root, and the new docstring claims otherwise
**`src/doc-system/site/dev-server.ts:936`** · lenses: security + blast-radius (deduped) · verified

`handleWriteSource` gates on `mayEditSource(resolved, await allowedSources())` (line 974). `handleReadSource` (936) resolves through `resolveInRepo` and nothing else, then returns `Bun.file(resolved).text()`. The route at 2143–2155 computes **one** `authorized` value and dispatches GET→read, POST→write — so anyone who can write can read, unconstrained beyond root containment.

Reachability holds: `csrfOk = viaTunnel || isSameOriginRequest(request)` and `mayWriteSource` returns `hasValidSession` when `viaTunnel`. `tls/key.pem` (1708 bytes, gitignored via `.gitignore:90 *.pem`) is present and readable this way, as are `.env` and `.git/config`. The magic link is documented in `dev-auth.ts` as a 5-minute bearer token that "LEAKS: browser history, Referer headers, reverse-proxy access logs, link previews in chat apps", redeemable for a 30-day session cookie. The invited capability is "look at and edit my docs on a headset"; the actual capability is "read my whole working tree".

The read breadth **pre-dates** this release (and `doc-site-system.md:333` does note it) — but the misleading docstring is new: `editable-sources.ts:2` says "WHICH files `editableSources` may touch" and `:25` says "Absolute paths the source editor may **read or write**", while no read-path caller references either export. The comment block above `resolveInRepo` (921–923) is also now stale ("Local dev only — your machine, your files… No auth/token"). Issue #128 is **closed** with the deferral recorded only in its closing comment; `grep 128 TODO.md` finds nothing, so nothing tracks it.

**Fix:** one line — `if (!mayEditSource(resolved, await allowedSources())) return 403` in `handleReadSource`. The editor only ever reads the source of a page in the corpus, which is exactly that set, so it is behaviour-neutral. If the deferral stands for this release, correct the docstrings to "may write" and note the read exposure in `doc-site-system.md`'s tunnel/invite section, and re-open a tracking item.

### F3 — `audit.blockOn` is a shipped, typed, documented option read nowhere; `audit-reach.ts` is imported by nothing
**`src/doc-system/site/audit-guard.ts:208`** · lens: correctness · verified

Repo-wide grep finds `blockOn` at exactly two sites: its declaration and one prose reference in `audit-reach.ts:23`. `auditDependencies` destructures only `cfg.level` and `cfg.allow`; the blocking decision is severity rank + gate matching, with no reach input. `audit-guard.ts` never imports `audit-reach.js`, and `audit-reach` is not re-exported from `src/doc-system/site/index.ts`. It **ships**: `dist/doc-system/site/audit-guard.d.ts:71` carries `blockOn?: 'severity' | 'runtime'` with the JSDoc.

The JSDoc's claim that "Build-only findings are LABELLED either way" is **false without qualification** — there is no reach field on `AuditAdvisory`/`AuditResult` and `reportAudit` prints no reach label. The consumer this was built for (the monorepo with 18 high/critical advisories, few runtime-reachable) sets `blockOn: 'runtime'`, type-checks clean, still fails on every build-only advisory with no label — and the next thing they reach for is `audit: false`, the exact outcome the option exists to prevent.

Fail direction is safe (the gate stays stricter than requested), and the other half of the same commit — the PASSING-ON-A-WAIVER marker — **is** wired and tested. TODO.md:94 still lists #56 as unchecked.

**Fix:** either wire `classifyReach` into `auditDependencies`/`reportAudit` and honour `blockOn`, or remove `blockOn` from the exported `AuditConfig` and the labelling sentence, leaving `audit-reach.ts` as tested groundwork.

### F4 — The published reference still says the dev server audits synchronously before binding and refuses to start; it now binds first and never throws
**`src/doc-system/doc-site-system.md:336, 344-347`** · lenses: correctness + blast-radius + security (three reports, deduped) · verified

`dev-server.ts:1650-1692` now reads "dependency audit (NON-BLOCKING — the port comes up first)" and fires `void auditDependencies(config.audit).then(...).catch(() => {})` after the bind; even `mode: 'fail'` only `console.warn`s. `buildSite`/`--test` still block synchronously, so the docs are *half* right — the dangerous kind.

`git diff v1.13.0...HEAD -- doc-site-system.md` touches no audit lines. Live text still reads "always **synchronously**… `devServer` audits just **before it binds the port** (a finding throws, so the server never comes up). The audit is sub-second" — contradicted by this release's own CHANGELOG (79.5s measured; `AUDIT_TIMEOUT_MS = 20_000`, fails open). It **ships**: `docs/doc-site-system/index.html:555`, `demo/docs.json`, and `OEBPS/doc-site-system.xhtml` in the built ePub. `CLAUDE.md:187` carries the same claim verbatim — and CLAUDE.md *was* edited in this diff without fixing it, so the next agent inherits it. `bin/dev.ts:84-87` repeats it.

An adopter running a tunnelled dev server with a known high advisory believes the gate refused to start. It did not.

*(One correction to the filed finding: the stale prose does **not** reach the npm tarball — `package.json` `files` excludes `src/`, `demo/` and `docs/`. It reaches adopters via the published site and the ePub.)*

**Fix (doc-only):** correct `doc-site-system.md`'s "When it runs" bullet and the `audit` config row, drop the "sub-second" premise, record the measured 79.5s / 20s-fails-open reality, and fix `CLAUDE.md:187` + `bin/dev.ts:86-87` in the same pass.
*Re-review scope: Tier 0 only.*

### F5 — #93: the machine-health guard now *sees* orphaned `--watch` bundlers but still cannot flag them; the changelog reads as coverage
**`src/doc-system/site/preflight.ts:119`** · lens: correctness · verified · severity flagged uncertain

Dropping the `--watch` exemption makes those processes candidates, but every trigger in `assessProcesses` is per-process RSS (`catastrophicPct` 50%, `hugeDevPct` 25%, `devLimitMb` 4096MB). Reproduced: 69 orphans × 194MB (the reporter's actual table, 13.09GB total, 5h old) → `{ level: 'ok', offenders: [], reason: '' }` at both 128GB and 32GB `totalRamMb`. The new tests are six `isDevProcess` unit assertions; nothing drives `assessProcesses` with a fleet of small orphans, so the suite is green and the reported machine still assesses `ok`.

Meanwhile `CHANGELOG.md:296` heads "The machine-health guard stopped exempting 13GB of orphans (#93)" and `preflight.ts:100-104` says "The guard looked, and deliberately skipped every one of them" (past tense) — both read as resolved.

Fair to the change: it does close a real gap (a `--watch` bundler past 4GB or 25% of budget is now visible where it was permanently exempt), and the reaping idiom added to `doc-site-system.md:492-519` addresses the leak at source.

**Fix (cheapest honest option):** one sentence in the CHANGELOG entry, the `isDevProcess` comment and `assessProcesses`' doc comment saying candidacy is not a trigger and that reaping, not the guard, is the fix for many small orphans. Optionally add a dev-scoped count or summed-RSS trigger (defensibly different from the all-process summed-RSS rule the comment rightly rejects), plus a test driving `assessProcesses` with the reporter's table.

---

## Follow-ups — file to `TODO.md`

Nothing here blocks the tag, but none of it may be dropped.

- [ ] **F6 (minor, correctness — confirmed during triage).** The Playwright lane is type-checked by **nothing**, while `CHANGELOG.md:37` heads "The test suite is type-checked now (#73)". Verified: `tsc -p tsconfig.tests.json --listFiles | grep -c /tests/` → **0**. `tsconfig.tests.json:16` includes only `src/**/*.ts` and `bin/**/*.ts`; root `tsconfig.json` covers `src/**` and `tsconfig.bin.json` only `bin/`. All 24 `tests/*.pw.ts` compile nowhere — including the two added in this diff, which lean on `(page: any)` helpers precisely because nothing objects. *Either add `tests/**/*.ts` to the include (fixing what it surfaces) or narrow the changelog to "the unit test suite".*
- [ ] **F7 (minor, correctness — confirmed during triage).** `insert-examples` unit coverage was **replaced rather than extended**. `git show v1.13.0:src/live-example/insert-examples.test.ts` had `describe('insertExamples grouping across the baked <script>')` and `describe('insertExamples fenced execution mode (:mode)')`; HEAD has only `describe('executable fence collisions (#139)')`. Five tests over still-shipping paths (`nextGroupableSibling` grouping across a baked `<script>`, `bakedJsForBlock` → `compiledJs`, prose-splits-a-group, first-fenced-mode-wins, contradictory-modes-console.error) are gone. Nothing else covers them at unit level. *Restore both `describe` blocks alongside the #139 tests — they test different things and both are cheap.*
- [ ] **F8 (minor, correctness — confirmed during triage).** `MenuItem` cannot express the drop-only item the runtime explicitly supports. `MenuAction.action` is required (`menu.ts:782`) but `filterForClick` branches on `if (action || menuItemsProvider)` (`menu.ts:901-903`) precisely so an `acceptsDrop` + `dropAction` item works, and `menu.ts:407-423` documents that shape. When the new typecheck lane compiled these for the first time, four literals in `src/menu.test.ts` (627, 641, 668, 685) were silenced with `as unknown as MenuItem` rather than the type being fixed — while the changelog says the 46 non-defect errors were "fixed honestly rather than with blanket `any`". *Make `action` optional on `MenuAction` (or add an explicit drop-target member to the union) and drop the four casts.*
- [ ] **F9 (minor, efficiency — unverified).** The 1.35 MB barrel win is guarded only by a literal-string check on `src/index.ts` (`src/index.test.ts:28`), which cannot see the realistic **transitive** regression — a code-editor field type in `schema-form/fields.ts`, a code preview in `value-renderer.ts`, `crud.ts` reusing `<tosi-code>`. No lane weighs `dist/index.js` (the build's two size lines cover the iife and hydrate.js) and there is no size budget in the repo. *Extend the test to bundle the barrel and read the sourcemap — `bun build dist/index.js --sourcemap=linked --external tosijs --external marked --external tjs-lang`, then assert `JSON.parse(map).sources` matches nothing in `/@codemirror\/|@lezer\/|tjs-lang|code-editor|live-example|doc-browser|doc-system\/doc-system/`. Costs ~1s, no byte threshold to drift, and the lens reports it passes today (0 heavy sources of 57) so it lands green. This is the technique CLAUDE.md already mandates for "what is actually in this bundle".*
- [ ] **F10 (minor, blast-radius — downgraded from major on verification; symptom reproduced here).** Live `ts` examples import an **unpinned** third-party module: `code-transform.ts:480` calls `fromTS(code, { emitTJS: true })` with no `typescriptUrl`, so tjs-lang's `DEFAULT_TYPESCRIPT_URL = "https://esm.sh/typescript@5"` applies. It is not in `bun.lock`, so `bun audit` cannot see it. This is the single hop in the chain that is neither same-origin nor pinned — everything else routes through `__TJS_LOCAL_BASE` or a `TJS_VERSION`-pinned CDN. *Corrections to the original filing: Bun refuses an http specifier as a file path, so there is **no** live network dependency in the unit lane and CI does not depend on esm.sh; and this repo's own corpus contains **zero** executable ```ts fences, so no reader of ui.tosijs.net is exposed today.* **However — I reproduced the error print** during `bun test`: `error: ENOENT reading "https://esm.sh/typescript@5"` (0 failures across the full run). It is newly surfaced by this diff, whose `insert-examples.test.ts` rewrite swapped a stub creator for the real `liveExample` component with a `ts` fence; the rejection leaks across test-file boundaries and prints under an unrelated file's header. *Two actions: (a) pass a pinned `typescriptUrl` (ideally the same-origin `/tjs/` copy the build already ships); (b) stub or settle the transform in `insert-examples.test.ts` so a rejection cannot land under another file.*
- [ ] **F11 (minor, blast-radius — downgraded on verification).** `buildSite` no longer resets `dist/` for `libraryBuild`/unconfigured consumers (`orchestrator.ts:427,772`), which is a real reduction in destructive footprint — but three **shipped** docs still promise the reset: `doc-site-system.md:1315-1318`, `doc-site-system.md:319` (config table), and `site-config.ts:162` "Runs before the dist dir is reset", which is present verbatim in `dist/doc-system/site/site-config.d.ts:132` and is what every adopter sees on editor hover. *Correction to the filing: the motivating consumer (tosijs) uses `libraryTsconfig`, so its `dist/` is still wiped; no surveyed adopter uses `libraryBuild`, and with no library configured tosijs-ui writes nothing into `dist/` to go stale — so the stale-artifact hazard is currently theoretical. Worth a separate check that #130 actually fixes the tosijs case it was filed for.* *Fix: correct the three doc sites and document under `libraryBuild` that the consumer now owns cleaning `dist/`.*
- [ ] **F12 (minor, blast-radius).** The `!` breaking removal of four root-barrel exports (`code-editor`, `doc-browser`, `doc-system/doc-system`, `live-example`) ships in a minor, so caret ranges pick it up on a routine install — and `import 'tosijs-ui'` silently stops registering `<tosi-code>`/`<tosi-doc-system>`/`<live-example>`, which fails at runtime, not at build time. Policy-compliant for this repo (CLAUDE.md says a breaking change earns a minor) and the CHANGELOG entry is thorough and shipped — but **`src/docs/migrating.md`**, the page `README.md:21` explicitly points upgraders at, has entries for 1.11.0/1.9.1/1.7.0/1.3.0 and **none for this one**. `README.md`, `Using-Components.md` and `llms.txt` are untouched by the entire release. Additionally **`src/doc-browser.ts:26` and `:117`** — the published doc page for `createDocBrowser` — still teach `import { createDocBrowser } from 'tosijs-ui'`, the now-broken import; they are display-only ```typescript fences so no doc test catches them, and the live-example lane structurally cannot (its import rewrite targets `window.xinjsui`, which `index-iife.ts` re-adds the cluster to).
- [ ] **F13 (minor, correctness).** `package.json` is still **1.13.0** while `CHANGELOG.md` declares `## 1.14.0 (unreleased)`; `src/version.ts` reads `1.13.0` and `docs/version.json` says `"generator": "1.13.0"`. Bump → `bun run build` → commit, in that order, per CLAUDE.md step 1 — otherwise 1.14.0 ships identifying itself as 1.13.0.
- [ ] **F14 (nit, security).** `src/doc-system/site/dev-auth.ts:309` still reasons from the retired alphabet: "Against 32⁷ ≈ 3.4 × 10¹⁰ that is ~111 years to exhaust", after the token became 22⁸ ≈ 5.4 × 10¹⁰. Conservative in the safe direction, but this is the one file where the numbers *are* the argument, and the adjacent test already asserts `8·log2(22) > 7·log2(32)`. *Update the exhaustion and in-window figures.*
- [ ] **F15 (nit, correctness + efficiency — deduped).** The headline barrel measurement is published as **three** different numbers. `src/index.ts:13-14` says 1.83 MB / 79%; `CHANGELOG.md:122-126`, `CLAUDE.md:348` and `src/index.test.ts:13` say 1.68 MB / 77%; an independent re-measurement (`bun build dist/index.js --minify`, tosijs/marked/tjs-lang external) gives 1.01 MB → 0.30 MB. All are plausible under different externals/minify settings and no document records which was used, so a maintainer re-measuring cannot tell a reproduction from a regression. `CHANGELOG.md` is in `files`, so two of the three ship. *Pick one methodology, record the exact command beside the number in `src/index.ts`, quote that single figure everywhere — or label one as the snowfox-app figure and one as the local reproduction.*

## Follow-ups — `UPSTREAM.md` + an issue on the upstream repo

- [ ] **U1 (from F10).** File against **tjs-lang**: `fromTS()`'s `DEFAULT_TYPESCRIPT_URL` is an unpinned major range (`https://esm.sh/typescript@5`) executed in every reader's browser, invisible to any consumer's lockfile and to `bun audit`. Ask for a pinned default and/or a documented same-origin seam (the equivalent of `__TJS_LOCAL_BASE` for the TypeScript compiler). Mirror the issue URL in `UPSTREAM.md` with the reproduction. *(Do not edit the tjs-lang repo directly.)*
- [ ] **U2 (housekeeping).** `UPSTREAM.md` already records tosijs#38 (`withAttributes` blocks downstream `.d.ts` emit), filed this cycle — confirm it is linked from the CHANGELOG write-up in B1 if it earns a `[note]`.

## Follow-ups — shared `tosijs-coding-practices`

- [ ] **P1.** `practices/review.md` — `release-check`'s "markdown-only diff" guard catches *claims without code*; this release produced three instances of the mirror failure, **code without a doc sweep** (F4 across three files, F11 across three files, F3's JSDoc asserting a labelling behaviour that does not exist). Propose a release-gate step: when a diff changes a documented *guarantee*, grep the canonical reference + `CLAUDE.md` + the emitted `.d.ts` for the old wording before tagging.
- [ ] **P2.** This repo's `CLAUDE.md` — fix line 187's audit-gate description as part of F4, and add a line to the "Where the design docs live" section noting that `doc-site-system.md` is *published* (39ff99d5 established this) and so must be swept whenever the behaviour it documents changes.

---

## Completeness gaps (carried forward verbatim where still true)

1. **The artifacts are not final.** Base ref is correct (tree clean, `v1.13.0` newest tag, 35 commits ahead) but `package.json`/`src/version.ts`/`docs/version.json` all still say 1.13.0. The bump + full rebuild is still ahead and will regenerate `dist/`, `docs/`, `iife.js(.map)`, the ePub, `llms.txt` and `version.json`. **Every generated file the lenses reviewed is pre-bump**, and `docs/` is already stale at HEAD — `docs/version.json` records commit `b7897148`, two commits back; `77f9295c` and `1d464991` rebuilt `dist/` but not the site.
2. **The doc-test gate itself changed in this diff, invalidating prior lane results.** `81f1d235` fixes `docsWithTests` selecting pages by substring (`doc.text.includes('```test')`), which counted 17 pages where 16 had tests and executed a prose page's examples in a background iframe — a documented false green. Any `test-browser` / `tests/doc-tests.pw.ts` result from before that commit is not evidence, and **nothing in this review shows the Playwright, haltija or `test-consumer` lanes were re-run at HEAD**. The same commit records a hole it left open: a runtime error in an example on a page with no ```test block is caught by nothing.
3. **`tosijs-ui/codemirror` identity is verified only where it cannot fail.** `src/codemirror.test.ts` compares `import('./codemirror.js')` against `import('@codemirror/view')` inside this repo's graph — identical by construction. Unchecked: (a) an adopter resolving `tosijs-ui/codemirror` against the lazily-imported `dist/code-editor-cm.js` (the #131 scenario); (b) the iife/CDN path, where CodeMirror is inlined into `dist/iife.js` so `tosijs-ui/codemirror` would be a **second copy** — and `index-iife.ts` does not spread the codemirror re-export onto `window.xinjsui`, leaving a CDN user with no identity-matched CM at all. `bin/smoke-consumer.ts` has no case for the new subpath, and that is the one lane whose job is export-map resolution as an adopter sees it.
4. **`export * from '@codemirror/state'` + `'@codemirror/view'` makes two third-party packages our public API.** A CodeMirror major becomes a tosijs-ui breaking change, with no advertised pin, no explicit re-export allow-list (`export *` re-exports whatever they add next), and no mention in `README.md` or `llms.txt`. **This is the ecosystem lens's beat and that lens did not run.**
5. **No lane exercises the bin CLI surface this release rewrites.** Four shipped bins (caddy-install, deploy-preview, tunnel, release-notes) moved onto one `parseArgv` that now **refuses unknown flags**, and `package.json`'s `deploy:index` flipped from `$PREVIEW_SSH` to `$PREVIEW_HOST`. CLAUDE.md already records that no lane runs a shipped bin's argument handling; `bin/argv.test.ts` covers the parser, not the wiring — so "a bin that ssh's into a host must not read an unknown flag as consent" is unverified end-to-end.
6. **`babylon-3d` gained a public attribute surface in the release that deprecates it, with zero tests.** `src`, `fov`, `clear-color`, `hero-light`, `toColor4()` and the auto-camera/auto-light fallbacks are new; `src/babylon-3d.test.ts` does not exist and the only way to exercise them is against the unpinned `https://cdn.babylonjs.com/babylon.js`. The deprecation names no removal version, and the new `console.warn` fires on every page rendering a `<tosi-3d>`, the doc site's own included.
7. **The deploy/host-config path is unit-tested only.** `orchestrator.ts` (+205), the new `host-preset.ts`, and `654b6453`'s firebase.json/outputDir check change what happens to a **consumer's committed deploy config**. No lane performs a real deploy and `test-consumer` scrubs `PREVIEW_HOST` by design, so the disagreement case is asserted by mocks and nothing else.
8. **`data-table.maxVisibleRows` went from a plain field to a getter/setter over a live DOM probe**, in the most-used component. `probeMaxElementHeight()` / `derivedMaxVisibleRows()` now decide the default at runtime instead of a flat 10000. Consumers who assign before connect, read expecting a number, or render under SSR/happy-dom are covered by unit tests only; `tests/table-row-cap.pw.ts` is new and was not run in this review, and nothing checks the probe's cost on a page with many tables.
9. **Coverage note.** The new `audit.blockOn: 'runtime'` reachability walk is a security-gate input tested only against synthetic manifests — nobody has run `classifyReach` against this repo's real lockfile to see what it labels. Defaulted off and conservative by construction, which is the right call; but see **F3** — it is also currently wired to nothing at all.

---

## Path to GO

1. Clear **B1** (write the 17 annotations; land the write-up commit with `[note]` bullets only).
2. Land **F1** and **F2** — both are small and both concern a security surface this release advertises as hardened; F2 is a one-liner.
3. Land **F3** and **F4** (both cheap; F4 is doc-only) and the one-sentence honesty fix in **F5**.
4. Bump to 1.14.0, `bun run build`, commit generated files (**F13** + gap 1).
5. Re-run **all four lanes at HEAD** — `bun test`, `bun playwright test`, `bun run test-browser`, `bun run test-consumer` — because the doc-test gate itself changed (gap 2).
6. Re-run `bun run release-check` as the **last** command before `git tag`.
7. File F6–F15 to `TODO.md`, U1–U2 to `UPSTREAM.md`, P1–P2 to the shared practices repo.

Re-review scope after remediation: **Tier 0** for the doc-only items (B1, F4, F11, F12, F15); **correctness + security over the remediation diff only** for F1/F2; **correctness** for F3/F5.